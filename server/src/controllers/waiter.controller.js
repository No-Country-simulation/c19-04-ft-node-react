import {
  createWaiter,
  requestWaiter,
  requestAttended,
} from "../services/waiter.service.js";

const WaiterController = {
  createWaiter,
  requestWaiter,
  requestAttended,
};

export default WaiterController;
