import {
    sendMessage,
    assignTables,
    getRequestedTables,
    deleteRequestTable,
    getWaiters,
} from "../services/waiter.service.js";

const WaiterController = {
    sendMessage,
    assignTables,
    getRequestedTables,
    deleteRequestTable,
    getWaiters,
};

export default WaiterController;
