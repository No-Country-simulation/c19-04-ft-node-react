import {
  createWaiter,
  requestWaiter,
  requestAttended,
  closeTable,
} from "../services/waiter.service.js";

const WaiterController = {
  createWaiter,
  requestWaiter,
  requestAttended,
  closeTable,
};

export default WaiterController;
