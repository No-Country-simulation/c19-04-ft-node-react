import {
	sendMessage,
	assignTables,
	getRequestedTables,
} from '../services/waiter.service.js'

const WaiterController = {
	sendMessage,
	assignTables,
	getRequestedTables,
}

export default WaiterController
