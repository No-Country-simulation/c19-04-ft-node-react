export const createCartItemObject = (_id, title, description, price, imgUrl, time) => ({
    _id: _id,
    title: title,
    description: description,
    price: price,
    imgUrl: imgUrl,
    time: time
});

